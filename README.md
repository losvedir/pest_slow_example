Demonstration of a slow (infinite?) pest parse.

```
$ cargo build --release
$ ./target/release/pest_slow
```

An actually slow (but I have still seen succeed eventually) file is provided as
`faster_but_still_slow.exs`.
